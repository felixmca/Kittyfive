/**
 * Colours the hero shares between its 3D camera and its SVG fallback. A module
 * of its own so the fallback (and Hero) never import three.js to get them.
 */

/** Kitty's collar-tag yellow, also --accent in globals.css. */
export const ACCENT = "#ffd166";
